// jv_sensors.cpp
// Sensor implementation is header-only (in jvlib.h) so sketch #defines are visible.
// This file exists only so the library still has a sensors translation unit if needed.
#include "jv_config_defaults.h"
#include "jvlib.h"
